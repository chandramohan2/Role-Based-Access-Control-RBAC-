Django==3.2.12              # Django 3.x version, compatible with Djongo
djongo==1.3.7               # Djongo version compatible with Django 3.x
gunicorn==20.1.0            # WSGI server for running Django
dnspython==2.2.1            # Required for mongodb+srv:// URI format (DNS resolution)
pymongo==3.11.4             # Use a compatible version of PyMongo with Djongo (3.11.x or 3.12.x)
whitenoise==5.3.0           # Optional, used for serving static files in production
