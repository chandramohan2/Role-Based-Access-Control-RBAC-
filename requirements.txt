Django==3.2.12              # Stable version of Django 3.x (compatible with Djongo)
djongo==1.3.7               # Djongo version that works with Django 3.x
gunicorn==20.1.0            # WSGI server to serve Django app
dnspython==2.2.1            # Required for mongodb+srv:// URI format (DNS resolution)
pymongo==3.12.1             # MongoDB driver (required for Djongo)
whitenoise==5.3.0           # For serving static files in production (optional)
